namespace SkyRoute.Domain.Models;

// Booking status (Confirmed / Cancelled / ...).
// In a real system there would be more statuses and transitions; today we keep it minimal.
public enum BookingStatus
{
    // Values pending implementation.
}
